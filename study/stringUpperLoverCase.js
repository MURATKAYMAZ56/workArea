var strng = [
    "her ne Kadar Delikanlilikta",
    " GOzumuz OLMasa da",
    "DALIMIZI KIRANIN AGACINI KOkten",
    "SOKERIZ"

];
sorted = [];

for (var i = 0; i < strng.length; i++) {

    sorted.push(strng[i].toLowerCase());
    sorted.replace(//,g);
};

console.log(sorted);